# apple mac osx macos count files notes

mac terminal count files in folder without ds_store

```bash
find . ! -name '.*' -type f | wc -l
```

<https://stackoverflow.com/questions/23325305/count-only-visible-files-in-directory>

---
